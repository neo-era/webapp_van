/**
 * Notes.gs — Ghi chú & tài liệu (sheet Notes + Google Drive).
 *
 * Dự kiến chứa:
 *  - getNotes(req)    : ghi chú của chính học sinh (có thể lọc theo môn/từ khóa).
 *  - saveNote(req)    : thêm/sửa ghi chú.
 *  - deleteNote(req)  : xóa ghi chú.
 *  - uploadFile(req)  : lưu file lên Google Drive (folder "THPT_Files"), trả link.
 *    Validate: ≤10MB, chỉ jpg/png/webp/pdf.
 *
 * TODO (Prompt 3.4): hiện thực ghi chú & upload.
 */
