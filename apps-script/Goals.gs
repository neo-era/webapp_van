/**
 * Goals.gs — CRUD mục tiêu theo môn và cập nhật tiến độ (sheet Goals).
 *
 * Dự kiến chứa:
 *  - getGoals(req)       : mục tiêu của chính học sinh.
 *  - saveGoal(req)       : thêm/sửa mục tiêu.
 *  - deleteGoal(req)     : xóa mục tiêu.
 *  - updateProgress(req) : cập nhật progress 0–100.
 *
 * TODO (Prompt 3.2): hiện thực CRUD mục tiêu & tiến độ.
 */
