/**
 * Exam.gs — Lộ trình ôn thi THPTQG (sheet ExamPlans).
 *
 * Dự kiến chứa:
 *  - getExamPlan(req)  : lấy kế hoạch ôn thi của học sinh (examDate, subjects, milestones).
 *  - saveExamPlan(req) : đặt/cập nhật ngày thi, tổ hợp môn, các mốc ôn tập.
 *
 * TODO (Prompt 3.3): hiện thực ôn thi.
 */
